# Handoff 2026-09-01 — аналитика была слепа полтора месяца, починена; GSC-срез; IE-шорт закрыт

## Где остановились
Три линии, все закрыты. (1) Разобран GSC-срез 01.09. (2) Вскрыто и починено главное:
**трекер Umami был недоступен клиентам с самого запуска 25.07** — порт 8444 закрыт облачным
файрволом IONOS. (3) Собран и залит шорт Image Enhancer, заход IE #2 закрыт целиком.

## 1. ГЛАВНОЕ: аналитика была сломана, все выводы «трафика нет» недействительны

**Причина: облачный файрвол IONOS.** Порт 8444 закрыт для всего интернета, а не «у части сетей»
и не из-за Китая. Три независимых доказательства:
1. Скан с нейтрального внешнего хоста (AdminVPS, Европа): 80/443/**8443** OPEN,
   8444/8446/9443 filtered.
2. На сервере фильтрации НЕТ: `ufw` inactive, `iptables INPUT` policy ACCEPT, пусто.
   Режет уровень провайдера — изнутри не видно.
3. В nginx-логах за ВСЁ время `script.js` запрашивали **8 раз, все 8 с IP 87.106.208.215**,
   то есть с самого сервера. Ни одного внешнего обращения никогда.

**Цепочка моей ошибки.** 10.08 Денис усомнился, что в базе всего 2 тестовых события. Я прогнал
`/probe-test` — но **с самого сервера на себя**, а такой тест физически не видит закрытый
снаружи порт. Выдал «всё исправно» и **не записал разговор в дневник**. 26.08 открыл ту же
проблему заново, тем же негодным методом, и заключил «движок исправен → значит трафика нет».
Полтора месяца стратегических выводов на данных, которых не могло существовать.

**Фикс (сделан, проверен).** Порт 8443 открыт и рабочий — через него уже ходят живые
пользователи расширений (`POST /ie/e` в логах). Добавлен `location /u/` в
`sites-enabled/pdfex-api.conf` → `127.0.0.1:3000/`; тег в 38 HTML переехал на
`https://api.devexthub.com:8443/u/script.js` + `data-host-url=".../8443/u"`.
Порт 8444 НЕ трогали — откат в одну строку. Бэкап: `/etc/nginx/pdfex-api.conf.bak-umami-1788242811`.

⚠️ **Отдельный вхост на 8443 заводить НЕЛЬЗЯ** — имя `api.devexthub.com` там уже держит бэкенд
PDF-to-Excel, второй такой же nginx игнорирует (`conflicting server name ... ignored`).
Первая попытка так и провалилась, откатили.

**ПОДТВЕРЖДЕНО ЖИВЫМ ЗАХОДОМ** 01.09 06:15:44 UTC (14:15 Китай): визит Дениса записался,
`url_path=/`. Первое за всю историю сайта событие от человека. Бонус: 8443 проходит из Китая.

**Что теперь честно:** поисковый трафик знаем по GSC (3 клика, измеритель независимый).
YouTube / PH / Custom GPTs / прямые заходы — **не мерили никогда**, счёт начинается с 01.09.

Побочно: **`https://devexthub.com` без www сломан** — отдаёт сертификат `CN=www.icloud.com`
(маскировка xray-REALITY на 443). Кто наберёт домен руками, до сайта не дойдёт. В offpage-
материалах ссылок без www 0 (сверено), ущерб ограничен. 443 освободить нельзя: REALITY не
умеет fallback по чужому SNI, это не «сложно», а невозможно.

## 2. GSC-срез 01.09 (данные до 29.08)
Разбор: `~/workspace/knowledge/gsc-devexthub-search-2026-09-01.md`.
- Показы: плато сломано вверх 172 → **210/день** (6253 всего). Клики те же **3**, последний 11.08.
- 🔴 Прирост дали ТОЛЬКО ключи с `chrome extension`. Крупные общие OCR-ключи **замёрзли в ноль**
  за неделю: jpg to text 169→169, jpeg to text 95→95, png to text 42→42 (позиции 85-96).
- 🟢 Первый толкаемый ключ: `ocr chrome extension` 6→14 показов, поз **38 → 23.4**.
  НЕ ПРОВЕРЕНО, какая страница ранжируется (кандидаты: blog/best-ocr-chrome-extension поз 42,
  лендинг ET поз 61.5) — смотреть фильтром Query+Page.
- ⚠️ Поправка к 25.08: вывод «image enhancer chrome extension на поз 9.9 не кликается → чинить
  сниппет» ПРЕЖДЕВРЕМЕНЕН. 19 показов на поз 9.3 дают ожидание 0.2-0.4 клика. Порог ~100 показов.
- Мост ET→PDF **выдохся**: за 7 дней ни одна из 5 оставшихся целей в индекс не вышла.
  Кластер закрыт до входящих ссылок.

## 3. YouTube — заход Image Enhancer #2 ЗАКРЫТ
- Лонг залит 31.08 = **youtu.be/dLNv9FL7DTA**.
- Шорт собран и залит 01.09 = **youtube.com/shorts/OuDq1EyaBgo** (18.4с, 1080x1920, голос Fenrir,
  без караоке-сабов, UTM `ie_short2`). Сборка, раскадровка, метаданные и грабли:
  `~/projects/Imageenhancer/releases/ie-anchor2/short/` (`build_ie_short.sh`, `metadata.md`).
- Проверено, а не предположено: RMS по секундам (звук до 17.5с, немого хвоста нет),
  прогон озвучки через Groq STT (промпт стиля не протёк).
- Два кропа пришлось переделать, оба брака вылезли только на чек-кадрах из готового файла.

## Следующий шаг
1. **Через ~5 дней (06-07.09) снять первые данные Umami** — впервые увидим реальные каналы.
   Смотреть: есть ли переходы с `utm_content=ie_short2` и `ie_long2`.
2. Заходы Дениса теперь тоже пишутся и на малых числах искажают — если начнёт мешать,
   завести исключение по IP (аналог DEV_IPS в Капитане). Пока НЕ заведено.
3. GSC — следующий замер ~15.09.

## Открытые вопросы (за Денисом)
- ⬜ Закреп-коммент под шортом OuDq1EyaBgo (текст в `short/metadata.md`), на youtube.com, НЕ в Studio.
- ⬜ Ротация YouTube: следующий заход за тем продуктом, кто дольше всех без лонга.
  Узкое место — футаж, удобнее записать пачку впрок.

## Контекст
- Правило записано в память: сомнение Дениса в цифрах = гипотеза о поломке ИЗМЕРЕНИЯ;
  доступность проверять ТОЛЬКО с внешнего хоста; сигнал об аномалии — сразу в дневник.
- Railway у Дениса НЕТ, несмотря на живой `RAILWAY_TOKEN` в окружении. Не предлагать.
- Грабля Umami: без браузерного User-Agent отдаёт `{"beep":"boop"}` и молча не пишет событие —
  curl без UA даёт ложный «не работает».

---

# Handoff 2026-08-27 — бренд-знак выбран и раскатан на прод

## Где остановились
Денис заметил, что в шапке до сих пор висит зелёный квадрат-заглушка, хотя концепты знака
рисовали ещё 07.08. Причина простая: концепты были, выбор Дениса — нет, работа замерла.
Выбрал **вариант B «Y», 3 узла** из пачки `_preview/logo-hub-v2.*` (я рекомендовал E — не угадал).
Знак собран, раскатан, проверен на живом проде. Открытым остался ОДИН вопрос — размер бренда
в шапке.

## Что сделано
1. `assets/logo-mark.svg` — основной знак (stroke 6) + `assets/logo-mark-small.svg`
   (утолщённый, stroke 10) + PNG 16/32/48/128/180. Пересборка: `node spike/render-logo.mjs`.
2. Шапка: **одна правка CSS** — `styles.css:50`, знак подставлен фоном к существующему
   `<span class="dot">`. HTML 38 страниц НЕ трогали, знак появился везде разом.
3. Фавикон врезан в `<head>` всех 38 страниц после `<meta charset>`: svg + png32 +
   apple-touch 180, пути абсолютные `/assets/...`. **До этого фавикона на сайте не было
   вообще ни одного** — во вкладке висела серая заглушка Chrome.
4. Запушено, проверено на проде по факту, не по факту пуша: `/assets/logo-mark.svg` → 200,
   `styles.css` на проде со свежей строкой 50, фавикон на подстранице блога — 3 ссылки.
   Коммиты f3d6af0 · 966d695 · c7c8597.

## Размер бренда — ЗАКРЫТ 27.08
Денис сомневался (хотел +30-40%), собрал сравнение трёх вариантов `_preview/brand-size.html`
(рендер — chrome-headless-shell по `http://localhost:8899/`, поднимать
`python3 -m http.server 8899` из корня проекта). Взяли **+15%: знак 26 / текст 20**
(`styles.css:49-50`, коммит 7c7b420, на проде). +35% отвергнут: бренд 24px давит навигацию,
оставшуюся на 15px — для «small tools that do one thing well» вид «стартап хочет казаться
крупнее, чем есть». Денис посмотрел живьём, принял. Фавикон во вкладке проверен по
скриншоту прода: силуэт «Y» читается.

## Следующий шаг
По бренду работы нет. Ближайшее в проекте — лонч Product Hunt по Image Enhancer
в пт 28.08, таймеры выставлены (см. запись ниже за 26.08 вечер).

## Грабли этого блока
- **Утолщённая версия знака годится ТОЛЬКО для 16px.** На 32px и выше узлы сливаются
  с центром в «кляксу»/мордочку — поймал на пиксельном сравнении, перерендерил 32px
  из тонкой. Записано в PROJECT.md.
- **playwright-core в этом окружении не стартует**: chromium-1208 падает с
  `chrome_crashpad_handler: --database is required`. Рабочий путь — `chrome-headless-shell`
  через CLI (`--screenshot`, `--window-size`, `--default-background-color=00000000`),
  как в `spike/render-logo.mjs`. Скрипты, которые ждут CDP :9222 (напр. `gallery-ph.mjs`
  в Imageenhancer), требуют уже запущенного chrome — это другой путь.
- Пуш увёл **14 коммитов, а не 3**: висели хвосты прошлых сессий (шорт ET, пакет PH,
  UTM на роликах). Все `[agent]`, чужого нет, но Денису сказано прямо.

## Хвост, не связанный с задачей
`_handoff.md` числился модифицированным ещё до начала работы (правка прошлой сессии),
я его не коммитил отдельно — разбирается вместе с этой записью.

---

# Handoff 2026-08-26 (вечер) — Product Hunt по Image Enhancer ЗАПЛАНИРОВАН на 28.08

## Где остановились
Пакет PH по Image Enhancer собран целиком, форма заполнена и отправлена Денисом в тот же день.
Лонч стоит в расписании PH на **пятницу 28.08.2026**, старт 00:01 PT = **07:01 UTC = 15:01
по Китаю**. До пятницы работы нет.

## Что сделано
1. Дата назначена Денисом (правило «только пятница» — PRODUCT-HUNT-RULES.md).
2. Галерея собрана: 4 слайда 1270×760 + thumbnail 240×240 →
   `~/projects/Imageenhancer/releases/ph-gallery/`, рендер `spike/gallery-ph.mjs`
   (headless chrome CDP :9222, пересобрать = `node spike/gallery-ph.mjs` из корня Imageenhancer).
3. Пакет полей обновлён: `offpage/product-hunt-image-enhancer.md` (коммиты 736eec5 · 9563f6b · b3626df).
4. First maker comment принят Денисом БЕЗ правок.
5. Три once-таймера в `~/.iia/schedules.json`:
   - `ph-ie-prelaunch-warmup` 28.08 06:40 UTC (14:40 Китая) — тексты для рассылки заранее
   - `ph-vote-collect-imageenhancer` 28.08 07:10 UTC (15:10) — проверить, что пост живой,
     выдать тексты с реальной ссылкой
   - `ph-ie-evening-check` 28.08 12:00 UTC (20:00) — позиция дня, неотвеченные комментарии,
     ЗАПИСАТЬ ИТОГ в пакет и дневник

## Следующий шаг
Ничего до пятницы, первый таймер сработает сам. Денису в 15:00 быть у телефона:
первые часы решают попадание в лидеры дня, отвечать на комментарии надо самому.

## Что ушло в форму (не переоткрывать)
- Ссылка первой = ЛЕНДИНГ с UTM, CWS вторым (трафик на свой домен, клики в Umami).
- Description ушла ПОЛНАЯ (359 симв.): лимит поля оказался 500, а не 260. В поле изначально
  лежал старый текст «Fix blurry, low quality photos» — запрещённое обещание, заменён до отправки.
- Shoutouts: Claude (alt Cursor) · GitHub (alt GitLab) · VS Code (ONNX Runtime в поиске PH нет).
- Open source — НЕ отмечено (open-source там модель, не код). X account — пропущен.
- ⚠️ ПРИНЯТЫЙ РИСК: в Video/Loom оставлен СТАРЫЙ ролик по IE до чистки позиционирования,
  может обещать «улучшить любое фото». Решение Дениса 26.08, не переспрашивать.
- ⚠️ CWS-описание НЕ переписано (решение 25.08, только со следующей заливкой версии).
- ⚠️ Замер органики после отключения Ads 23.08: считать по 27.08 включительно, дальше разрыв.

## Грабля, которая стоила бы дорого
Материал, лежавший в плане как основа главного слайда (`releases/analog-test/A-digital-jpeg-*`,
256×256 JPEG q30), визуально НЕПРИГОДЕН: метрика x4.07 честная, но глазами восковая мазня.
Взята пара `testdata/degraded_squirrel.jpg` → `enhanced_squirrel_check.jpg` (400×267 → 1600×1068).
Замер на НЕЙ (`spike/measure-gallery-pair.mjs`): выход x1.28 к входу, bicubic x0.60,
**модель против простого ресайза x2.13** ← эту цифру и называем публично, x4 резкости НЕ называть.
Правило: цифра и картинка обязаны быть с ОДНОГО файла; демо-материал = умеренно ужатое,
НЕ убитое в хлам. Подробно: `Imageenhancer/knowledge/model-limits-analog-vs-digital.md`.

---

# Handoff 2026-08-26 (день) — аналитика пуста, GSC Coverage сдвинулся, ET-лонг залит

## Где остановились
День шёл по трём линиям: аналитика (нашли, что она пуста), GSC Coverage (пришёл свежий срез,
разобран) и YouTube-заход по Extract Text (лонг залит). Все три закрыты, хвостов два —
шорт ET и субтитры.

## Что сделано

**Аналитика — главная находка дня.**
UTM на ютуб-роликах не стояли НИ РАЗУ, ни на одном из 12. Полез в Umami (она на 127.0.0.1:3000
того же IONOS, креды root-only через ssh — агенту доступна, ключей у Дениса НЕ просить).
Движок исправен: сквозной POST /api/send вернул sessionId, сертификат LE до 28.09, script.js 200,
тег на лендингах на месте. Но в `website_event` за всё время 2 записи, обе служебные.
Реальных визитов НОЛЬ. Сверил с GSC — расхождения нет, значит не поломка, а отсутствие трафика.
Своё тестовое событие `/probe-pafnutiy` из прода удалил (DELETE 1).
Вторая дыра, крупнее первой: в закрепе ET стояла только ссылка на CWS — не наш домен,
трекера там нет, переход не виден в принципе.

**GSC Coverage 26.08 (данные до 21.08), сравнение с baseline 12.08.**
В индексе 23 → 25 (+2 скачком 18.08: smallpdf-alternative + iphone-transcribe, сошлось
с Performance-срезом). Не в индексе 12 → 10. ГЛАВНОЕ: `Discovered - not indexed` 11 → 0,
вместо него `Crawled - not indexed` 9. Google дошёл до страниц и не взял.
Трактовка (помечена как интерпретация, не факт): к «мало авторитета» добавилась оценка
контента, одними ссылками эти 9 могут не открыться.
Тревогу по показам из baseline СНЯЛИ: провал 384→94 был волатильностью, дальше плато
~184/день две недели, позиция ровно 62-66.
Разбор: `~/workspace/knowledge/gsc-devexthub-coverage-2026-08-26.md`.

**YouTube — заход Extract Text закрыт.**
Лонг ЗАЛИТ: youtu.be/YBuAdO9E4cU (27.4с, ключ copy text from image 5400/KD21, голос Kore).
Подсказка на 00:22 → ET-лонг #1 = youtu.be/G8w0kwg5Glc (ID наконец зафиксирован, раньше
дважды искали глазами по превью).
Конечные заставки не ставили: при 27.4с end screen сел бы поверх прожжённой энд-карточки
24.2-27.4 и подсказки.
UTM вписаны в метаданные всех релизов; схема упрощена до `_long` / `_short` на продукт
вместо уникального слага на ролик.

**Массовый проход по закрепам старых роликов ОТМЕНЁН** (решение Дениса, я согласился).
Час работы при нуле переходов; закреп на ролике с 2-8 просмотрами не увидит никто.
Правим точечно и только там, где просмотры есть — кандидат PDF-шорт первого захода (544).
Тексты закрепов по всем 4 продуктам выданы в чат и лежат в metadata.md релизов.
Грабля: свои комментарии НЕ видны в Studio → Комментарии (там чужие) — смотреть и править
на обычном youtube.com, иначе плодятся дубли (Денис так и написал второй).

## Следующий шаг
1. Шорт по Extract Text из УЖЕ отснятого футажа: голос Fenrir, крупные drawtext,
   караоке-сабов НЕТ (рецепт залетевшего PDF-шорта). Новая съёмка не нужна, режу я.
2. Дальше по кругу ротации — Image Enhancer (хвосты: шорты Upscale 4x и Unblur,
   голос-тексты в `releases/ie-anchor/_handoff.md`).

## Открытые вопросы (за Денисом)
- Субтитры к ET-лонгу: `subs.srt` пословный (127 блоков по 0.1-0.2с, под караоке в кадре),
  для YouTube CC не годится. Предложил пересобрать фразами 3-6с из `groq_words.json` — ответа нет.
- Точечно поправить закреп у PDF-шорта первого захода (единственный с реальными просмотрами).
- ~~Product Hunt по Image Enhancer — дату не назначили~~ → НАЗНАЧЕНА в тот же вечер на 28.08,
  см. верхний блок хендоффа.

## Контекст
- Следующий замер GSC ~08.09. К нему смотреть: сдвинулись ли 9 Crawled после PH.
  Если нет — вопрос не в ссылках, а в тонкости 7 висящих PDF-статей, тогда объединять,
  а не дописывать.
- Новые статьи под ключ НЕ писать: при Crawled-not-indexed ещё одна похожая статья вредит.
- Коммиты дня: ad9d392, b344a3b, 26934ad (проект) · 3636b97, 24ae450, bf27f54, cb26017,
  5d680f3, f9fb978 (workspace).

---
# АРХИВ — предыдущие хендоффы

# Handoff 2026-08-20 — Шорт Transcribe #1 собран (Fenrir), ждёт заливки

## Где остановились
Собран и утверждён Денисом первый шорт по Transcribe из футажа якорного лонга.
Готов к заливке, метаданные под ключ выданы. Кода продуктов не трогали.

## Готово
- `releases/transcribe-anchor/short/tr-short-final.mp4` — 14.5с, 1080x1920, 2.8 МБ, −14 LUFS.
- Метаданные под ключ: `releases/transcribe-anchor/short/metadata.md` (title / описание / теги / закреп-коммент / раскадровка).
- Сборка: `releases/transcribe-anchor/short/build_transcribe_short.sh`. Футаж = `releases/transcribe-anchor/src/compressed_2026-08-16_14-25-10.mp4` (тот же, что у лонга).
- Коммиты: e0c1830 (сборка+метаданные), d0b6530 (хендофф), 3e0ee18 (фикс моста), d693ece (пересборка на Fenrir), b6fd58e (память+скилл).

## Структура шорта (14.5с)
лента длинных роликов YouTube «35 MINUTES EACH / NOBODY HAS TIME» 2.2с → плеер зум 1.35x «SO DO NOT WATCH / READ IT INSTEAD» 1.56с → кнопка Transcribe «ONE CLICK» 1.28с → панель целиком, транскрипт en с таймкодами «FULL TEXT / WITH TIMESTAMPS» 2.5с → та же панель на ja «SWITCH THE / LANGUAGE» 2.38с → Word «EXPORT TO WORD» + плашка «Full tutorial on my channel» 1.98с → энд-карточка 2.5с

## Ключевые решения этой сессии
1. **Сабов-караоке НЕТ.** Проверил по коду: у залетевшего PDF-шорта (544 просмотра/сутки) их не было — `releases/shorts/build_short.sh` = только крупные drawtext сверху/снизу + голос. Пословные сабы через Groq появились позже и только на ЛОНГАХ. Два текстовых слоя в 15с = шум.
2. **Хук поменян против плана в хендоффе.** Не кроп плеера, а правая лента YouTube со стеной длительностей (1:00:49 / 19:30 / 31:04 / 26:27 / 37:02). Боль видна за полсекунды, без слов.
3. **Голос = Fenrir**, выбран Денисом на слух из 4 сэмплов (Puck / Fenrir / Laomedeia / Sadachbia). Puck остаётся на лонги. Записано в скилл `vertical-short-builder`.

## МОЯ ОШИБКА, Денис поймал — два типа ютуб-ссылок
Написал в метаданных «ссылка в описании не кликается». Неверно. Разделять:
- **ВНУТРЕННЯЯ youtu.be** — кликается БЕЗ верификации с самого начала. Проверено 03.08: шорт PDF v4 залит с мостом на `youtu.be/I_7uDO1Qtfc`. Это главный мост шорт→лонг.
- **ВНЕШНЯЯ на лендинг** — под время-гейтом, только закреп-коммент.
Я сам это советовал 03.08, потом потерял при переносе в заметку 17.08. Исправлено в metadata.md, releases/transcribe-anchor/_handoff.md, memory/2026-08-17.md, MEMORY.md. Мост теперь ПЕРВОЙ строкой описания.

## ГРАБЛИ (новые, в общий рецепт)
- **Стиль-промпт TTS протекает в аудио.** Длинная многофразовая инструкция → Puck и Fenrir зачитали её вслух («Huge energy, fast pace, punchy delivery...»), длительность 14с → 18-21с. Стиль = ОДНА фраза с двоеточием: `Say with maximum enthusiasm, like a hyped tech YouTuber, fast, punchy and genuinely excited: <текст>`. Проверять НЕ на слух, а прогоном через Groq STT.
- **`-shortest` при муксинге** обрезал видео по длине голоса и съел энд-карточку. Правильно: `apad` на аудио + `-t <длина видео>`.
- **`crop=W:H:x:y` после `scale=W:-2`** падает («Invalid too big size») — `-2` округляет высоту непредсказуемо. Писать `crop=1080:ih:190:0`.
- **Кропы под вертикаль** (переиспользовать): панель целиком `crop=445:910:1460:108` (аспект 0.489, ложится в 9:16); лента роликов `crop=455:815:965:200` (0.558, почти ровно 9:16); Word без риббона и имени владельца `crop=880:830:545:135`.

## Следующий шаг — за Денисом
Залить шорт. При заливке:
1. Описание/title/закреп из `metadata.md`, ссылка на лонг `Gv99xOSFys8` первой строкой.
2. **Проверить поле Related video** — 03.08 было закрыто молодому каналу, статус на 20.08 НЕИЗВЕСТЕН. Открылось → поставить Gv99xOSFys8 и сказать мне, зафиксирую.
3. Not made for kids, Public.

## Дальше по ротации
Следующий продукт — **PDF-to-Excel**. Футаж уже есть: `releases/shorts/pdf-to-excel-raw.mp4` (тот, из которого резали залетевший v4). Предложил Денису собрать шорт под ДРУГИМ углом (сканы вместо цифрового PDF) на Fenrir — ответа пока нет.
Хвост углов по Transcribe на следующий оборот круга: экспорт SRT, «Any video» (Vimeo/Udemy/Zoom), 37 000 знаков конспекта.

---

# Handoff 2026-08-16

## План на завтра (16→17.08)
Возвращаемся к Transcribe Video to Text (Capitan). Делаем ДЛИННЫЙ ролик по одному из готовых кластеров, потом shorts из того же материала.
- Кластеры уже собраны (5 блог-статей в Devexthub-site/blog/, НОВЫЙ ресёрч НЕ нужен):
  1. can-chatgpt-transcribe-a-video
  2. how-to-transcribe-a-tiktok-video
  3. how-to-transcribe-a-video-on-iphone
  4. how-to-transcribe-a-zoom-recording
  5. how-to-transcribe-song-lyrics-from-a-video
- Ядро длинного ролика — берём широкий кластер (TikTok или iPhone), если по нему норм. Финальный выбор кластера подтвердит Денис завтра.
- Ротация контента (задал Денис 16.08): Transcribe → PDF-to-Excel → Extract Text по кругу, каждый продукт = пара «длинный ролик + shorts». Заливает Денис сам, я готовлю ролик + тексты (title/описание из ключей/теги/закреп).

## Статус на сегодня
- Image Enhancer шорт Enhance — ГОТОВ и ЗАЛИТ на YouTube (@NicholaChaus). Файл releases/ie-anchor/shorts/short_enhance_v1.mp4.
- TTS-движок: Gemini Puck (~0.25 цента/ролик, проверено). Бесплатный запас Edge-TTS (venv /home/client/projects/edge-tts-venv). Тайминги сабов через Groq STT, НЕ Deepgram (риск гранта).
- Хвост: 2 шорта IE (Upscale 4x, Unblur) не сделаны — не срочно, дрипать позже.

## ПРАВИЛО (Денис требует, 16.08) — отвечать ИЗ ФАКТОВ, не из головы
Как только речь о проекте / нюансах / «что у нас было, что собрано, что поменялось» — СНАЧАЛА лезу в память проекта и файлы, СВЕРЯЮСЬ, и отвечаю фактами. Денис НЕ обязан помнить сколько ключей/кластеров собрано — это моя работа держать и доставать. Не выдумывать «нужен новый ресёрч» и т.п. когда всё уже лежит в проекте. Не гонять его перепроверять зафиксированное.

## 16.08 (вечер) — Transcribe заход утверждён
- Денис согласовал сценарий: широкий ролик «How to get a transcript of any YouTube video — free, in Chrome» под ключ youtube transcript generator free (2400/KD29) + how to get a youtube transcript. Старый ролик Transcribe признан неудачным («мутный»), новый = замена по факту.
- Внутрь широкого — вставка 10с про Auto-detect языков (идея Дениса про «экзотический язык» отклонена как тема ролика: нулевой англоспрос, языкового кластера в ресёрче нет; берём как фичу-вставку).
- ФАКТ ИЗ КОДА (sidepanel.js:268-271): чип Language показывается ТОЛЬКО на Deepgram-пути — `isDeepgramPlatform = !info.isYouTube && !captionTracks?.length`. На YouTube чипа НЕТ. → языковой блок снимать на НЕ-YouTube видео БЕЗ субтитров.
- Ждём футаж OBS от Дениса (2 блока + дропдаун языков крупно). Монтаж/озвучка/тексты — мои.
- Шорт IE Enhance: 28 просмотров за ~3-4 часа (воскресенье, Азия-обед). Для сравнения PDF-шорт v4 = 544/сутки.

### ПОПРАВКА (Денис прав, я ошибся) — на YouTube выбор языка ЕСТЬ
Два РАЗНЫХ дропдауна, я их смешал:
1. `deepgramLangSelector` (чип AUTO) — только Deepgram-путь (не YouTube, нет сабов). Выбор языка РАСПОЗНАВАНИЯ, ~25 языков.
2. `langSelector` (Language: select) — YouTube/нативные сабы. Это ПЕРЕВОД: content.js:221 подставляет через &tlang= 12 языков — en, pt, es, hi, ru, ja, de, fr, it, ko, zh-Hans, ar + оригинальные дорожки видео.
ОГОВОРКА (content.js:209): переводы добавляются ТОЛЬКО если трек взят обычным путём, не pot-путём (`!baseUrl.includes("pot=")`). На видео под анти-ботом/VPN список будет без переводов → перед записью проверить, что в дропдауне 12+ языков.

### Сюжет ролика уточнён (идея Дениса) — учебная лекция
Материал в кадре = длинная лекция/учебный туториал (40+ мин), боль «всю информацию не удержишь».
ВАЖНО: саммари/конспекта в расширении НЕТ (греп по extension-src пустой) — не обещать «готовый конспект».
Честная подача: полный текст → Ctrl+F по термину → экспорт Word → вставил в ChatGPT для конспекта (это ещё и добивает наш кластер can-chatgpt-transcribe-a-video).
Языковой блок теперь СНИМАЕТСЯ НА YOUTUBE (перевод лекции на хинди/испанский) — отдельное не-YouTube видео больше НЕ нужно.

## ФУТАЖ Transcribe ПОЛУЧЕН (16.08, 14:25) — releases/transcribe-anchor/src/compressed_2026-08-16_14-25-10.mp4
122с, 1920x1080, 30fps, ~12МБ (в git НЕ коммичу, бинарь). Кадры-превью: releases/transcribe-anchor/frames/.
Материал в кадре: YouTube «Claude Code - Full Tutorial for Beginners» (Tech With Tim, 35:18, 1.4M просмотров) — учебный туториал, ровно та боль что хотел Денис.
Хронометраж:
- 0:00-0:15 сайдпанель, кнопка Transcribe, счётчик «13 AI transcriptions left · YouTube unlimited»
- ~0:20 транскрипт EN готов: 148 paragraphs, таймкоды, кнопки Copy / .doc (Word) / Export
- ~0:40 переключение языка → «Loading subtitles»
- ~1:00 японский (ja), 84 paragraphs
- ~1:20 окно Downloads → запуск Word
- ~1:40 испанский (es), 149 paragraphs
- ~1:50 ОТКРЫТЫЙ .doc в Word: испанский текст с таймкодами [0:34][0:50]..., 37 400 знаков ← кульминационный кадр
ГРАБЛИ футажа (лечу кропом): русский интерфейс Word/Windows + имя «Денис Торопов» в шапке Word + таскбар с Яндексом. Кроп верх/низ решает.
НЕ снято: Ctrl+F по термину, экспорт SRT. Обхожусь без них, доснимать не прошу.

## ЗАВТРА 17.08 — первым делом
Transcribe-лонг `releases/transcribe-anchor/tr-anchor-final.mp4` (34с) аппрувнут, заливает Денис завтра.
ПЕРЕД ЗАЛИВКОЙ написать ему: title + описание + теги + текст закреп-коммента (ключ youtube transcript generator free 2400/KD29, ссылка = лендинг /transcribe-video-to-text). Детали в releases/transcribe-anchor/_handoff.md.

## 16.08 (вечер) — GSC-срез + 3 статьи best-*-chrome-extension ВЫЛОЖЕНЫ
Повод: GSC Performance экспорт 16.08 (данные 27.07-14.08). Разбор → ~/workspace/knowledge/gsc-devexthub-search-2026-08-16.md.
Вывод: домен ранжируется ТОЛЬКО на запросах со словом extension/chrome/chromebook (поз 10-15), общие ключи (jpg to text) — поз 87, мусор. Образец окупаемости = best-ocr-chrome-extension (608 показов, поз 36.5).
Написаны и ЗАЛИТЫ (push 7416a17, все три отдают 200):
- /blog/best-video-transcript-chrome-extension/
- /blog/best-image-enhancer-chrome-extension/
- /blog/best-pdf-to-excel-chrome-extension/
Формат по образцу best-ocr: критерии выбора → наш продукт по шагам → CTA с UTM → честный блок про конкурентов → перелинковка на свои how-to. FAQPage JSON-LD на каждой. Карточки в blog/index.html + 3 записи в sitemap.
Честность (специально): в Transcribe-статье НЕ обещаю приватность (не-YouTube идёт через облако), в Enhancer-статье прямо сказано что безнадёжный расфокус не восстановить.
Следующий замер отдачи — через 2-3 недели по GSC (позиции этих 3 URL + показы по extension-ключам).


# ============ ИТОГ ДНЯ 16.08.2026 ============

## Что закрыто сегодня
1. Transcribe якорный лонг СОБРАН И АППРУВНУТ: releases/transcribe-anchor/tr-anchor-final.mp4 (34с, 1080p, −14 LUFS). Футаж от Дениса (Claude Code туториал 35:18), голос Gemini Puck, сабы Groq. Скрипт сборки build_transcribe_anchor.sh, детали и грабли — releases/transcribe-anchor/_handoff.md.
2. GSC Performance разобран (данные 27.07-14.08) → ~/workspace/knowledge/gsc-devexthub-search-2026-08-16.md. Главное: домен ранжируется ТОЛЬКО на extension/chrome/chromebook-запросах (поз 10-15), общие ключи поз 80-90 = мусор.
3. Написаны и ЗАЛИТЫ 4 статьи (все отдают 200):
   - /blog/best-video-transcript-chrome-extension/
   - /blog/best-image-enhancer-chrome-extension/
   - /blog/best-pdf-to-excel-chrome-extension/
   - /blog/how-to-copy-text-from-an-image-on-a-chromebook/ (ключ уже на поз 9.7 БЕЗ статьи, лучшая позиция сайта)
4. Построен кросс-кластерный мост: 4 контекстные ссылки из сильных ET-страниц в висящий PDF-кластер + ссылка с PC-статьи на Chromebook-статью.
5. Таймер: weekly GSC-срез, понедельник 11:00 Китая (id gsc-devexthub-weekly, ~/.iia/schedules.json).

## ЗАВТРА 17.08 — единственная горящая задача
Перед заливкой ролика написать Денису: title + описание + теги + текст закреп-коммента.
Ключ: youtube transcript generator free (2400/KD29) + how to get a youtube transcript. Ссылка везде = лендинг https://www.devexthub.com/transcribe-video-to-text (НЕ CWS). Напомнить про закреп-коммент (в описании ссылка не кликается, время-гейт канала). Тексты в <code>-блоках, без тире.

## Отложенные хвосты (НЕ горит)
- Шорты из Transcribe-футажа (языки / экспорт в Word / 35 мин → текст) — следующий оборот круга.
- Хвост IE: шорты Upscale 4x и Unblur, голос-тексты готовы в releases/ie-anchor/_handoff.md.
- Через 2-4 недели проверить: вышел ли PDF-кластер из «Discovered, currently not indexed» после моста, и попали ли 4 новые статьи в индекс. Если PDF не сдвинулся — замораживаем кластер, дело во внешних ссылках.
- ВАЖНО не забыть: перелинковка НЕ объясняет разрыв PDF/ET (посчитано, плотность у PDF даже выше). Не повторять вывод «нашли причину».


# ============ 21.08.2026 (вечер) — правила CWS поменялись + отзыв для PDF-to-Excel ============

## Где остановились
Денис прислал пост Google от 20.08 (developer.chrome.com/blog/cws-review-updates-2026). Разобрали, что из этого бьёт по нам, обновили память. Затем собрали три варианта английского отзыва на PDF-to-Excel для знакомого Дениса.

## Что сделано
1. **Разбор поста CWS.** Четыре изменения:
   - Featured Badge сворачивают в 2026, **самономинация закрыта с 20.08**. Наша заявка по Transcribe (30.07) мертва, ответа не будет.
   - **Лимит 2 расширения на аккаунт.** Опубликованные грандфазерятся: наши 4 остаются, лимит выставят = 4. Пятый продукт потребует запроса квоты в Developer Dashboard.
   - **Рейтинг взвешивает свежие отзывы** (как Google Play). Старые 5★ больше не держат карточку, свежий негатив бьёт сильнее.
   - **Автопроверка установки при заливке zip** + обещание вернуть нормальные сроки ревью (актуально для висящих IE 1.0.6 и ET 1.2.4).
2. `knowledge/cws-featured-badge-guide.md` помечен ⛔ УСТАРЕЛО в шапке, чтобы через месяц никто не полез подавать.
3. MEMORY.md: строка CWS-заявок переписана (featured мёртв), добавлена строка про лимит 2 расширения и рейтинг по свежим отзывам. Дневник 2026-08-21 дополнен. Коммит 840896e.
4. Три варианта отзыва на PDF-to-Excel (тексты — в ~/projects/Pdftoexel/_handoff.md). Факты от Дениса: знакомый конвертировал банковскую выписку, раньше набивал руками. AI-кнопку не упоминаем (не подтверждено, а в 1.0.6 лимит 5/день — обещать unlimited нельзя).

## Следующий шаг
Ждём, что знакомый опубликует отзыв. Дениса просить не надо, мяч у него.

## Вывод, который стоит применить (из смены правил рейтинга)
Раз свежие отзывы весят больше — запрос отзыва надо ставить строго ПОСЛЕ успеха. Для Image Enhancer это `download_click`, а не `enhance_done`: из 6 дошедших до результата файл скачали только 2, остальные разницы не увидели. Просить у них отзыв = собирать свежий негатив в новой системе рейтинга.

## Открытые хвосты (не трогали сегодня)
- IE 1.0.6 и ET 1.2.4 на ревью CWS.
- PDF-to-Excel v1.0.7 собрана (zip от 20.08), ждёт живого теста Дениса и заливки. Номер не поднимать.
- Шорт Transcribe #1 ждёт заливки Денисом на YouTube.
- ~/projects/_TO_DELETE — карантин до ~26.08, удаление за Денисом (rm в терминале VS Code).

## Справка по сайту (перенесено из MEMORY.md dream-циклом 26.08.2026)

- Хостинг: GitHub Pages, НЕ IONOS (порт 443 занят VPN). Домен www.devexthub.com живой с TLS
  (CNAME на Pages, с 25.07). Репо раздаётся публично: коммит = публикация.
- Аналитика: Umami self-host на IONOS, `/opt/umami`, docker, дашборд
  https://api.devexthub.com:8444 за basic-auth. Креды: `/opt/umami/BASICAUTH_CREDS.txt`,
  логин самой Umami в `ADMIN_CREDS.txt`. Агенту доступна напрямую с сервера, ключи не просить.
- Состав: витрина + 4 лендинга + блог 18 статей, все под ключи Semrush, ссылки с UTM.
  Техническое SEO с 28.07: sitemap.xml (24 стр) + robots.txt. Homepage URL в CWS-листингах
  вписаны Денисом.
- **FAQPage-разметка на 32 страницах с 07.05.2026 НЕ актив.** Google полностью деприкейтнул
  FAQ rich results: в июне 2026 снят отчёт в GSC и Rich Results Test, в августе отключён API.
  Блока в выдаче не будет ни у кого, аргумент «остаётся для госсайтов и health» это
  устаревшая информация 2023-2026. Сносить существующую разметку не надо (не вредит,
  Bing/Perplexity/ChatGPT её парсят), но новым страницам добавлять ради SEO бессмысленно.


# ============ 03.09.2026 — заход Transcribe #2 (подкаст) закрыт ============

## Где остановились
Лонг залит, шорт собран и ждёт заливки. Плюс попутно вскрылись две вещи по аналитике.

## Что сделано
1. **Очередь ротации.** Денис назвал Transcribe — верно. Даты лонгов: Transcribe 17.08 ·
   PDF 22.08 · ET 26.08 · IE 31.08. В памятке висела строка «дальше PDF-to-Excel» — это был
   порядок ПО СПИСКУ, отменённый поправкой 25.08. Поправил, правило: очередь по ДАТАМ ЛОНГОВ.
   Заодно исправил дату PDF-лонга: 22.08, а не 25.08 (25-е — дата шорта).
2. **Ключ.** TikTok (480/KD15) отвергнут не по SEO — у Дениса tiktok.com не грузится, футаж
   снять нечем; ключ остался неиспользованным. Взят подкаст-кластер (пробит 01.08):
   `transcribe podcast` 480/30, `podcast transcription service` 390/18,
   `transcript generator for apple podcasts` 140/26.
3. **Статья выложена:** https://www.devexthub.com/blog/how-to-transcribe-a-podcast/ (200,
   в sitemap, карточка в блоге). Честные лимиты написаны прямо в тексте: потолок сессии
   40 мин, подкаст ест месячный AI-лимит, безлимит только YouTube и Vimeo.
4. **Apple Podcasts подтверждён живьём** — впервые за историю проекта (в БД apple 0, spotify 1).
   Диаризация SPEAKER 1/2/3 работает. Язык интерфейса Apple держит в ПУТИ URL: `/ru/` → `/us/`.
5. **ЛОНГ ЗАЛИТ: https://youtu.be/i-4eQkuy6Bk** (26.4с, Puck, подсказка на 0:20 на лонг #1
   Gv99xOSFys8, UTM `tr_long2`). Сборка: `releases/transcribe-anchor2/`.
6. **ШОРТ СОБРАН, НЕ ЗАЛИТ:** `releases/transcribe-anchor2/short/tr-short-final.mp4`
   (14.6с, Fenrir, результат-первым). Тексты — `short/metadata.md`.

## Следующий шаг
За Денисом: залить шорт + поставить ДВА закрепа (на лонге и на шорте, тексты в metadata.md,
ставить на youtube.com, НЕ в Studio).
Дальше круг едет к PDF-to-Excel — заход = НОВЫЙ ЛОНГ под новый ключ, нужен свежий футаж.

## ОТКРЫТЫЙ ВОПРОС, решать Денису
У ЗАЛИТОГО ЛОНГА тот же хвост тишины, что я убрал в шорте: озвучка кончается на 22.1с,
видео идёт до 26.4с = 4.3с молчания. Пересобрать в ~23.6с можно за 5 минут, но перезаливка
означает удалить youtu.be/i-4eQkuy6Bk и залить заново (как было с IE 31.08).
Сам не трогаю — это удаление опубликованного.

## Мои косяки дня (все поймал Денис)
1. Счётчик кредитов в сценарии — убран из UI в v1.2.3 (30.08), лежало в `Capitan/knowledge/versions.md`.
2. «Сажусь за статью» без выполнения в том же ходу. Правило записано в память проекта.
3. Просил доснять кусок с открытым Word — **кадр уже был в футаже на 1:38**. Вытащил 17 превью,
   посмотрел 7, вывод написал по неполной выборке. ПРАВИЛО: смотреть ВСЕ извлечённые кадры.
4. Просил «дать эпизоду доиграть 13 минут» ради длинного транскрипта — расшифровка идёт
   в реальном времени, это время Дениса и минуты гранта ради двух секунд в кадре. Снято.
5. Стартовая карточка «Transcribe a Podcast» читалась как имя продукта. Правило: первая
   строка стартовой карточки = ПОЛНОЕ ИМЯ РАСШИРЕНИЯ.
6. Хвост тишины в шорте (17.4с при озвучке 12.81с). Правило: длина видео = голос + adelay + 1.5с.

## Аналитика — две находки
- **Umami прозрела только 01.09** (фикс файрвола IONOS). Мерить переходы с YouTube физически
  стало можно 1.09 в 06:15 UTC. За полтора суток 13 сессий: прямые 6 · google 3 ·
  **Chrome Web Store 2** · AlternativeTo 1 · ProductHunt 1. С YouTube — ноль, но окно
  наблюдения двое суток и роликов с UTM всего три. Вывод делать НЕ на чем.
- 🔴 **Ссылки Chrome Web Store = `rel="ugc nofollow"`** (проверено по живому HTML листинга).
  Запись «вес добьёт бэклинк из CWS (DR92)» в `knowledge/seo-keywords-transcribe.md`
  ОПРОВЕРГНУТА и исправлена. SEO-веса стор не даёт вообще, но приводит ЖИВЫХ людей —
  второй источник после google. Реферер приходит только доменом, какое расширение дало
  клик, не определить.

## Срез по лонгам 03.09 (9 роликов, 227 просмотров)
71 IE Upscaler 13.08 · 52 YouTube Transcript 17.08 · 25 ET 26.08 · 21 PDF 02.08 ·
18 ET 04.08 · 17 IE 31.08 · 15 старый Transcribe 18.06 · 8 PDF bank statement 22.08 ·
0 подкаст 03.09.
Возраст объясняет не всё: 02.08 и 04.08 старше лидера. Оба лидера под ШИРОКИЕ ключи,
аутсайдер под узкий сценарный. Гипотеза, не факт — проверять на следующих заходах.
